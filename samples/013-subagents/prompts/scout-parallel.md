Use the delegate tool with its `tasks` form and exactly two independent tasks.
Both agents must be `scout`. Ask one scout to find the production marker
`WAREHOUSE_REGION` and its exact relative path in the fixed tiny fixture. Ask
the other scout to find the test marker `EXPECTED_SKU_COUNT` and its exact
relative path. Return the ordered delegation result and explain why the child
processes have isolated context windows.
