# Copy the values you need into a real `.env` kept OUTSIDE the git repo
# (e.g. one directory above this folder). `env.ts` walks up the tree to find it.
# Provide at least one provider key.

ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
